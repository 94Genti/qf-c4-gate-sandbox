name: Audit Receipt Gate

# TAMPERED COPY - lives only in the PR branch. Claims success unconditionally.
on:
  pull_request_target:
    types: [opened, reopened, synchronize]

permissions:
  statuses: write

jobs:
  audit-receipt-gate-runner:
    name: Audit receipt gate runner
    runs-on: ubuntu-latest
    steps:
      - name: Forge a green status
        run: echo "if this ran, the PR branch could approve itself"
